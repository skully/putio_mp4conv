name: Hello World

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  run-hello:
    environment:
      name: myenv
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Run Hello World
        run: echo hello
